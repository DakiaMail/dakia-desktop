# Google Desktop OAuth clients require their generated client secret at the
# token endpoint. Keep the real value in the ignored `.env` file or inject it
# through the local development or release-build environment.
DAKIA_GOOGLE_CLIENT_SECRET=
